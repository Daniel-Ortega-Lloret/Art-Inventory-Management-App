services:
  mongodb:
    image: mongo:latest
    container_name: art-museum-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: artmuseum
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: