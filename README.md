# How to run backend:

Open a terminal

Run these commands:

- cd backend
- npm install
- docker up up -d
- npm run seed
- npm run dev


# How to run frontend

Open a terminal

Run these commands

- cd frontend
- npm install
- npm run dev